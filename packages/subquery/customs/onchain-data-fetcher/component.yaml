name: onchain-data-fetcher
author: subquery
version: 0.1.0
type: custom
description: A tool that retrieves and processes on-chain data based on user requests in natural language.
license: Apache-2.0
aea_version: '>=1.0.0, <2.0.0'
fingerprint:
  __init__.py: 
  onchain-data-fetcher.py: 
fingerprint_ignore_patterns: []
entry_point: onchain-data-fetcher.py
callable: run
dependencies:
  openai:
    version: ==1.11.0
  tiktoken:
    version: ==0.5.1
